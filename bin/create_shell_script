#!/usr/bin/env sh

err() {
	printf "%s\n" "$1"
	exit 1
}

[ -z "$EDITOR" ] && err "EDITOR is not set"
[ -z "$1" ] && err "no argument given"

printf "#!/usr/bin/env sh\n\n" > $1
chmod +x $1
$EDITOR $1
